# Archived step-10 diagnostics (pre 9-level migration)

These were one-off diagnostics from the 8-level / lower-middle-upper era:
- `fig8_direct_decomp.py`   — 2×3 direct-vs-piecewise PV-advection comparison
- `fig8_computed_pv.py`     — computed-PV vs ERA5-PV overestimate investigation
- `fig8_extended_cali_compare.py` — extended-domain vs Cali-crop experiment

They reference the old `middle` piece key and outdated-domain assumptions and are
NOT part of `run_all.sh`. Archived during the 9-level (1000…100) + surface/lower/
upper restructure. Kept for reference; would need piece-key/level updates to run.
